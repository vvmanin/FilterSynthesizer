# FilterSynthesizer — Roadmap

Work-item state for planned features. The binding cross-tier rules every item
must respect live in `docs/CONTRACTS.md`; the tier model and file map live in
`docs/ARCHITECTURE.md`.

All earlier hardware-synthesis items (dispatch gate, 1st-order, HP, notch, BP,
MFB, Ackerberg-Mossberg) have landed or been dropped; their history is in git
and in the other `dev/*.md` notes.

---

## Backlog

_No open items._
